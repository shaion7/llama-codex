# Core PyTorch (Matches your 4070 Ti / CUDA 12.6)
torch==2.9.1 --index-url https://download.pytorch.org/whl/cu126

# Hugging Face Ecosystem (Verified stables for Llama 3.2 + LoRA fine-tuning)
transformers==4.57.6
peft==0.18.1
accelerate==1.12.0
datasets==4.5.0
trl==0.27.0
bitsandbytes==0.49.1  # Optional optimization for memory efficiency
huggingface_hub

# Operations & Logging (Matched to your MLflow 3.7.0 deployment)
mlflow==3.7.0
boto3==1.36.3  # Stable for MinIO/S3 compatibility

# Additional Utils (Optional but included for potential data ops; remove if unused)
scipy==1.14.1
einops==0.8.1
protobuf==5.29.4  # For MLflow and gRPC in Istio/K8s
requests==2.32.5